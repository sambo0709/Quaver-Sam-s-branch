{
  "name": "quaver",
  "version": "1.0.0",
  "description": "A mood-based music recommendation app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.mjs",
    "clean": "rm -rf dist",
    "test": "node --test tests/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:all": "node --test tests/*.test.js tests/integration/*.test.js"
  },
  "dependencies": {
    "@sentry/node": "^10.73.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "esbuild": "^0.28.2",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.1",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^7.1.0"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.0.0",
    "supertest": "^7.2.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
